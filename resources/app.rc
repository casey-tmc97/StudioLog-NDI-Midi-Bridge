// Windows resource file — embeds the application icon into the EXE so
// Explorer, the taskbar, and Alt+Tab show it rather than the generic
// Windows application icon.
IDI_ICON1 ICON "NDI_BRIDGE.ico"
